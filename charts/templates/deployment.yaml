apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "repo-test-infra-front.fullname" . }}
  labels:
    app: {{ include "repo-test-infra-front.name" . }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.syncWave | default "0" | quote }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit | default 2 }}
  progressDeadlineSeconds: 300
  selector:
    matchLabels:
      app: {{ include "repo-test-infra-front.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "repo-test-infra-front.name" . }}
        {{- if .Values.istio.enabled }}
        version: {{ .Values.image.tag | quote }}
        {{- end }}
      annotations:
        checksum/chart: {{ print (toYaml .Values) (.Files.Glob "**" | toString) | sha256sum | quote }}
        {{- if .Values.istio.injection }}
        sidecar.istio.io/inject: "true"
        {{- end }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.env.PORT | default 8080 }}
              protocol: TCP
          env:
          {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
          {{- end }}
          {{- if .Values.existingSecret }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret }}
                  key: DATABASE_URL
            - name: RSA_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret }}
                  key: RSA_PRIVATE_KEY
          {{- end }}
        
          {{- if .Values.resources }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5